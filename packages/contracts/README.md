# packages/contracts

Shared data contracts: JSON Schema, OpenAPI, and generated clients. Single source
of truth for the agent-response and recommendation contracts (see
`../../docs/PHASE_WISE_BUILD_PLAN.md` §4).
